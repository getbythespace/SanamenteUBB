name: proyecto_tesis
description: Proyecto de tesis para monitoreo del estado de ánimo

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  get_it: ^8.0.2
  firebase_core: ^3.6.0
  firebase_auth: ^5.3.1
  cloud_firestore: ^5.4.4
  equatable: ^2.0.3
  internet_connection_checker: ^0.0.1
  shared_preferences: ^2.0.15
  intl: ^0.17.0
  http: ^0.13.4
  flutter_bloc: ^8.0.1
  dartz: ^0.10.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
